<%= div_for comment do %>
	<p>
		<strong>
			Posted <%= time_ago_in_words(comment.created_at) %> ago
		</strong></br>
		<div><%= comment.created_at %></div>
		<div><%= comment.body %></div>
	</p>
<% end %>